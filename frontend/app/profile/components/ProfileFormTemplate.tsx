import React, { ReactNode } from "react";

export default function ProfileFormTemplate({
  formHeader,
  children,
}: {
  formHeader: string;
  children: ReactNode;
}) {
  return <div>ProfileFormTemplate</div>;
}
